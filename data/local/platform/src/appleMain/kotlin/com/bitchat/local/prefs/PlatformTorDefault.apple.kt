package com.bitchat.local.prefs

import com.bitchat.domain.tor.model.TorMode

/** Unchanged, so iOS behaviour is untouched by the desktop fix. */
internal actual val platformDefaultTorMode: TorMode = TorMode.ON
